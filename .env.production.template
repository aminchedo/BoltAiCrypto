# HTS Trading Dashboard - Production Environment Variables
# Version: 1.0.0
# Last Updated: October 7, 2025

# =============================================================================
# CRITICAL: Set these before deploying to production
# =============================================================================

# -----------------------------------------------------------------------------
# Frontend Configuration (VITE_* vars are exposed to browser)
# -----------------------------------------------------------------------------

# API Base URL (backend REST API)
# Example: https://api.yourdomain.com
# Leave empty to use relative URLs (requires nginx proxy)
VITE_API_BASE_URL=

# WebSocket URL (real-time data)
# Example: wss://api.yourdomain.com/ws
# Leave empty to use relative URLs (requires nginx proxy)
VITE_WS_URL=

# Environment identifier
# Must be "production" for production builds
VITE_ENV=production

# Application title (shown in browser tab)
VITE_APP_TITLE=HTS Trading System

# Debug mode (MUST be false in production)
VITE_DEBUG=false

# -----------------------------------------------------------------------------
# External API Keys (Optional - for enhanced features)
# -----------------------------------------------------------------------------

# CoinMarketCap API (for market data)
# Get key: https://pro.coinmarketcap.com/account
VITE_COINMARKETCAP_API_KEY=

# CryptoCompare API (for additional data sources)
# Get key: https://www.cryptocompare.com/cryptopian/api-keys
VITE_CRYPTOCOMPARE_API_KEY=

# -----------------------------------------------------------------------------
# Feature Flags (Optional)
# -----------------------------------------------------------------------------

# Enable real-time market scanner
VITE_FEATURE_REALTIME_SCANNER=true

# Enable AI predictions
VITE_FEATURE_AI_PREDICTIONS=true

# Enable strategy backtesting
VITE_FEATURE_BACKTESTING=true

# Enable trade execution (set to false until paper trading is implemented)
VITE_FEATURE_TRADE_EXECUTION=false

# -----------------------------------------------------------------------------
# Analytics & Monitoring (Optional but Recommended)
# -----------------------------------------------------------------------------

# Sentry DSN (error tracking)
# Get DSN: https://sentry.io
VITE_SENTRY_DSN=

# Google Analytics ID (if using GA)
VITE_GA_TRACKING_ID=

# Plausible domain (if using Plausible Analytics)
VITE_PLAUSIBLE_DOMAIN=

# -----------------------------------------------------------------------------
# Performance & Optimization
# -----------------------------------------------------------------------------

# Maximum number of data points in charts (prevent memory issues)
VITE_MAX_CHART_POINTS=1000

# WebSocket reconnect interval (milliseconds)
VITE_WS_RECONNECT_INTERVAL=5000

# API request timeout (milliseconds)
VITE_API_TIMEOUT=30000

# Cache duration (milliseconds)
VITE_CACHE_DURATION=300000

# -----------------------------------------------------------------------------
# Backend Configuration (Not exposed to browser)
# -----------------------------------------------------------------------------

# Backend API port (internal use only)
API_PORT=8000

# WebSocket port (internal use only)
WEBSOCKET_PORT=8765

# CORS origins (comma-separated, include production frontend URL)
CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Database connection string
# Example: postgresql://user:password@db.yourdomain.com:5432/hts_trading
DATABASE_URL=

# Redis URL (for caching, optional)
# Example: redis://redis.yourdomain.com:6379/0
REDIS_URL=

# -----------------------------------------------------------------------------
# Exchange API Keys (Backend only, NEVER expose to frontend)
# -----------------------------------------------------------------------------

# Binance API (for real trading data)
BINANCE_API_KEY=
BINANCE_API_SECRET=

# KuCoin API (optional, for additional liquidity)
KUCOIN_API_KEY=
KUCOIN_API_SECRET=
KUCOIN_API_PASSPHRASE=

# Coinbase Pro API (optional)
COINBASE_API_KEY=
COINBASE_API_SECRET=
COINBASE_API_PASSPHRASE=

# -----------------------------------------------------------------------------
# Authentication & Security (Backend only)
# -----------------------------------------------------------------------------

# JWT secret key (generate with: openssl rand -base64 32)
JWT_SECRET_KEY=

# JWT algorithm
JWT_ALGORITHM=HS256

# Access token expiration (minutes)
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Refresh token expiration (days)
REFRESH_TOKEN_EXPIRE_DAYS=7

# -----------------------------------------------------------------------------
# Notifications (Optional)
# -----------------------------------------------------------------------------

# Telegram Bot (for alerts)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Discord Webhook (for alerts)
DISCORD_WEBHOOK_URL=

# Email SMTP (for notifications)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=noreply@yourdomain.com

# -----------------------------------------------------------------------------
# Logging & Monitoring (Backend only)
# -----------------------------------------------------------------------------

# Log level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# Log format (json or text)
LOG_FORMAT=json

# Sentry DSN (backend error tracking)
SENTRY_DSN_BACKEND=

# -----------------------------------------------------------------------------
# Real-Time Agent Configuration (Backend only)
# -----------------------------------------------------------------------------

# Enable real-time market scanner agent
REALTIME_AGENT_ENABLED=true

# Scan interval (milliseconds)
REALTIME_SCAN_INTERVAL_MS=1000

# Maximum concurrent scans
REALTIME_MAX_CONCURRENT_SCANS=10

# =============================================================================
# DEPLOYMENT CHECKLIST
# =============================================================================
#
# Before deploying, ensure:
# [ ] All VITE_* variables are set correctly
# [ ] VITE_ENV=production
# [ ] VITE_DEBUG=false
# [ ] API keys are production keys (not test keys)
# [ ] DATABASE_URL points to production database
# [ ] JWT_SECRET_KEY is strong and unique
# [ ] CORS_ORIGINS includes production domain
# [ ] Sentry DSN configured (for error tracking)
# [ ] Backup .env file to secure location (password manager)
#
# =============================================================================

# =============================================================================
# SECURITY NOTES
# =============================================================================
#
# 1. NEVER commit this file with real values to git
# 2. Use environment variable injection via hosting provider (Vercel, Netlify, etc.)
# 3. Rotate API keys regularly (every 90 days)
# 4. Use different keys for staging and production
# 5. Monitor API key usage for anomalies
# 6. Backend keys (BINANCE_*, JWT_*, etc.) should NEVER be in VITE_* vars
#
# =============================================================================
